#include "catch.hpp"
#include "Smile.h"

/*
TEST_CASE("Smile Domain", "[smile]")
{
  //create a point
  Point aPoint = Point();
  double x = aPoint.x;
  double y = aPoint.y;
  int id = aPoint.id;
  
  //test construction of point
  SECTION("Point Construction")
  {  
     CHECK(x == 0.0);
     CHECK(y == 0.0);
     CHECK(id == -1);
  }
}
*/
